/**
 * Default route id for legacy global capability pages that redirect into the
 * project-scoped hub. Matches unified registry routeId for PM-WORKBENCH.
 */
export const LEGACY_BRIDGE_ROUTE_ID = 'pm-workbench';
